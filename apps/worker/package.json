{
  "name": "@repo/worker",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@repo/contracts": "*",
    "@repo/domain": "*",
    "bullmq": "^5.71.0",
    "ioredis": "^5.10.0"
  }
}
