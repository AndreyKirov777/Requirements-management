version: "3.9"

services:
  postgres:
    image: postgres:18
    environment:
      POSTGRES_DB: requirements_management
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
